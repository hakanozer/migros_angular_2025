import { PtitleDirective } from './ptitle.directive';

describe('PtitleDirective', () => {
  it('should create an instance', () => {
    const directive = new PtitleDirective();
    expect(directive).toBeTruthy();
  });
});
